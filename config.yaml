data:
  sources:
    # updated from documentation issue 3
    climate:
    health:
    disasters:
  paths:
    raw: "data/raw"
    processed: "data/processed"
models:
  lstm:
    epochs: 100
    batch_size: 32
    learning_rate: 0.001
  xgboost:
    n_estimators: 200
    max_depth: 5
evaluation:
  metrics: ["accuracy", "precision", "recall", "f1", "mae", "rmse"]
